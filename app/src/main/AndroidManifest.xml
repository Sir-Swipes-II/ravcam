<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <permission
        android:name="com.ravcam.vcam.permission.ACCESS_FEED"
        android:protectionLevel="signature" />

    <application
        android:name=".RavCamApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RavCam">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="RavCam VCam Module" />
        <meta-data
            android:name="xposedminversion"
            android:value="100" />
        <meta-data
            android:name="xposedsharedlibrary"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RavCam"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Injected consumers run with the scoped app's UID, so the Messenger
          endpoint validates an LSPosed-published capability instead of applying
          the signature permission at the component boundary.
        -->
        <service
            android:name=".feed.service.RavFeedService"
            android:exported="true" />

        <provider
            android:name=".feed.provider.RavFeedContentProvider"
            android:authorities="com.ravcam.vcam.feed"
            android:exported="false"
            android:grantUriPermissions="true">
            <grant-uri-permission android:pathPrefix="/session/" />
        </provider>
    </application>

</manifest>
